// import Slider from 'modules/slider';

/*window.addEventListener('load', function () {
    const imageSlider = new Slider('.slider');
    const $slider = document.querySelector('.slider');

    $slider.querySelector('.prev').addEventListener('click', function () { imageSlider.prev(); });
    $slider.querySelector('.next').addEventListener('click', function () { imageSlider.next(); });

    document.querySelector('.open-slider').addEventListener('click', e => {
      $slider.className = $slider.className.replace('off', '').trim();
    });
    document.querySelector('.close-slider').addEventListener('click', e => {
      $slider.className += ' off';
    });
});*/